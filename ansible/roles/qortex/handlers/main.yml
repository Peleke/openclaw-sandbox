---
- name: Restart qortex container
  become: true
  ansible.builtin.shell: |
    docker restart {{ qortex_docker_container }} 2>/dev/null || true
  changed_when: true

- name: Reload systemd
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
